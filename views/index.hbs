{{#extend 'layout'}}
  {{#content 'body'}}
    <h1>{{title}}</h1>

    <div class='cards'>
      {{#each items}}
        <div class='card'>
          <h3>{{this.title}}</h3>
          <p>{{this.description}}</p>
        </div>
      {{/each}}
    </div>
  {{/content}}
{{/extend}}